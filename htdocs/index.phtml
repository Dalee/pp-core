<?php
	include_once '../lib/mainuser.inc';

	$engine = new PXEngineIndex();

	$engine->init();

	$engine->runModules();

	$engine->fillLayout();

	$engine->sendHeaders();

	$engine->profilerStop();

	$engine->html();
?>
